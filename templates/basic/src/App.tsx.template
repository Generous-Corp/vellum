import { HomeScreen } from "../ui/generated/Home.generated";
import { Design, useState } from "@vellum/ui";
import { importedDesign } from "@vellum/imported";

export function App() {
  const [count, setCount] = useState(0);
  if (importedDesign) {
    return (
      <Design
        document={importedDesign}
        actions={{ "main/create-button-v1": () => setCount((value) => value + 1) }}
      />
    );
  }
  return (
    <HomeScreen
      title={"{{PROJECT_NAME_JSON}}"}
      count={count}
      onIncrement={() => setCount((value) => value + 1)}
    />
  );
}
