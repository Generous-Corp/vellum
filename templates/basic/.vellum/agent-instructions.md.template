# Vellum agent contract v1

- Treat `framework.lock` as tool-owned and `sources/imported/` as immutable
  source snapshots.
- Treat `design/ir/`, `design/generated/`, `tokens/imported/`,
  `assets/generated/`, and `ui/generated/` as tool-owned output.
- Treat `design/overlays/` as developer-owned behavior and customization that
  import/reimport must preserve byte-for-byte.
- Put application behavior, state, and safe extensions in `src/` and
  `components/`.
- Treat `app.toml`, `package.json`, and `package-lock.json` as developer-owned
  manifests, while preserving their validated schemas and exact pins.
- Never silently discard unsupported import properties. Preserve diagnostics
  and conversion losses with the imported snapshot.
- Pass supported Pulp `.pulp.zip` Figma exports directly to `vellum import` or
  `create --from figma`; do not unpack or rewrite the archive. Its SHA-256 and
  original bytes are part of the immutable source evidence.
- Use stable imported component identities; do not replace generated identities
  with names based on array position.
- Keep platform-specific work in `platforms/` and optional native extensions in
  `native/`.
- Run scenarios and visual captures after imports, reimports, or layout changes.
- This template contains no media or audio subsystem. Add optional capabilities
  explicitly rather than assuming they exist.
