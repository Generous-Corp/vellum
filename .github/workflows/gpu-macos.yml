name: Locked macOS GPU proof

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]
    tags: ['v[0-9]*']

permissions:
  attestations: read
  contents: write

concurrency:
  group: vellum-gpu-${{ github.ref }}
  cancel-in-progress: false

jobs:
  gpu-macos-arm64:
    name: gpu-macos-arm64
    runs-on: macos-15
    steps:
      - name: Check out full preserved Vellum history without credentials
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Download and verify the locked Skia and Dawn archive
        shell: bash
        run: |
          set -euo pipefail
          archive="$RUNNER_TEMP/skia-build-mac-arm64-gpu-release.zip"
          curl --fail --location --silent --show-error \
            https://github.com/danielraffel/skia-builder/releases/download/chrome/m150/skia-build-mac-arm64-gpu-release.zip \
            --output "$archive"
          printf '%s  %s\n' \
            13b0e9818c3b05db661af85cb1e2bf2ef10e30d468b81351dd90295237d17734 \
            "$archive" | shasum -a 256 -c -
          printf 'VELLUM_SKIA_ARCHIVE=%s\n' "$archive" >> "$GITHUB_ENV"

      - name: Set up the authoring toolchain
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: packages/vellum-ui/package-lock.json

      - name: Build the real TypeScript and JSX host fixture
        run: |
          npm ci --prefix packages/vellum-ui
          npm test --prefix packages/vellum-ui
          npm run --prefix packages/vellum-ui build:native-test -- \
            "$RUNNER_TEMP/vellum-ui-native.iife.js"

      - name: Build the release GPU slice
        run: |
          cmake -S . -B build-gpu \
            -DCMAKE_BUILD_TYPE=Release \
            -DVELLUM_BUILD_TESTS=ON \
            -DVELLUM_REQUIRE_GPU=ON \
            -DVELLUM_AUTHORING_TEST_BUNDLE="$RUNNER_TEMP/vellum-ui-native.iife.js" \
            -DVELLUM_SKIA_ARCHIVE="$VELLUM_SKIA_ARCHIVE"
          cmake --build build-gpu --parallel

      - name: Install the pinned Emscripten SDK
        shell: bash
        run: |
          set -euo pipefail
          emsdk="$RUNNER_TEMP/emsdk"
          git init "$emsdk"
          git -C "$emsdk" remote add origin https://github.com/emscripten-core/emsdk.git
          git -C "$emsdk" fetch --depth=1 origin \
            7f06d88dc04b71a08d0c275df9cb68337e7128fd
          git -C "$emsdk" checkout --detach FETCH_HEAD
          "$emsdk/emsdk" install 6.0.2
          "$emsdk/emsdk" activate 6.0.2
          printf 'VELLUM_EMSDK=%s\n' "$emsdk" >> "$GITHUB_ENV"

      - name: Build the source-matched browser payload
        shell: bash
        run: |
          set -euo pipefail
          export EMSDK_QUIET=1
          source "$VELLUM_EMSDK/emsdk_env.sh"
          python3 scripts/build_web_payload.py \
            --source-commit "$GITHUB_SHA" \
            --output "$RUNNER_TEMP/web-payload" \
            --json > "$RUNNER_TEMP/web-payload.json"

      - name: Verify renderer provenance and byte identity
        run: |
          python3 tools/provenance/verify_renderer_dependency.py \
            --archive "$VELLUM_SKIA_ARCHIVE" \
            --require-macho

      - name: Run native, capture, and sterile-consumer tests
        run: |
          ctest --test-dir build-gpu --output-on-failure
          ./build-gpu/apps/gpu-native/vellum-gpu-native.app/Contents/MacOS/vellum-gpu-native \
            --self-test --capture "$RUNNER_TEMP/vellum-gpu-proof.png" \
            | tee "$RUNNER_TEMP/vellum-gpu-proof.txt"
          file "$RUNNER_TEMP/vellum-gpu-proof.png"
          shasum -a 256 "$RUNNER_TEMP/vellum-gpu-proof.png" \
            > "$RUNNER_TEMP/vellum-gpu-proof.sha256"

      - name: Prove the installed SDK artifact is reproducible
        shell: bash
        run: |
          set -euo pipefail
          first="$RUNNER_TEMP/sdk-first"
          second="$RUNNER_TEMP/sdk-second"
          node_binary="$(command -v node)"
          node_version="$(node -p 'process.versions.node')"
          node_platform="$(node -p 'process.platform')"
          node_arch="$(node -p 'process.arch')"
          node_dist="node-v${node_version}-${node_platform}-${node_arch}.tar.gz"
          node_url="https://nodejs.org/dist/v${node_version}/${node_dist}"
          node_dist_sha="$(curl -fsSL "https://nodejs.org/dist/v${node_version}/SHASUMS256.txt" | awk -v name="$node_dist" '$2 == name { print $1 }')"
          test -n "$node_dist_sha"
          node_license="$(node -p "require('path').join(require('path').dirname(require('path').dirname(process.execPath)), 'LICENSE')")"
          test -s "$node_license"
          NODE_BINARY="$node_binary" NODE_LICENSE="$node_license" NODE_VERSION="$node_version" \
            NODE_URL="$node_url" NODE_DIST_SHA="$node_dist_sha" python3 - <<'PY'
          import hashlib, json, os
          from pathlib import Path
          digest = lambda path: hashlib.sha256(Path(path).read_bytes()).hexdigest()
          record = {
              "schema": "vellum.node-runtime-provenance.v1",
              "name": "Node.js",
              "version": os.environ["NODE_VERSION"],
              "target": "darwin-arm64",
              "source_url": os.environ["NODE_URL"],
              "distribution_sha256": os.environ["NODE_DIST_SHA"],
              "binary_sha256": digest(os.environ["NODE_BINARY"]),
              "license_file": "LICENSE",
              "license_sha256": digest(os.environ["NODE_LICENSE"]),
          }
          Path(os.environ["RUNNER_TEMP"], "node-provenance.json").write_text(
              json.dumps(record, indent=2, sort_keys=True) + "\n"
          )
          PY
          python3 scripts/build_sdk_artifact.py \
            --skia-archive "$VELLUM_SKIA_ARCHIVE" \
            --web-payload "$RUNNER_TEMP/web-payload" \
            --node-binary "$node_binary" \
            --node-license "$node_license" \
            --node-provenance "$RUNNER_TEMP/node-provenance.json" \
            --output-dir "$first" --json > "$RUNNER_TEMP/sdk-first.json"
          python3 scripts/build_sdk_artifact.py \
            --skia-archive "$VELLUM_SKIA_ARCHIVE" \
            --web-payload "$RUNNER_TEMP/web-payload" \
            --node-binary "$node_binary" \
            --node-license "$node_license" \
            --node-provenance "$RUNNER_TEMP/node-provenance.json" \
            --output-dir "$second" --json > "$RUNNER_TEMP/sdk-second.json"
          archive="vellum-sdk-0.1.0-darwin-arm64.tar.gz"
          cmp "$first/$archive" "$second/$archive"
          cmp "$first/SHA256SUMS" "$second/SHA256SUMS"
          (cd scripts && shasum -a 256 -c INSTALLER_SHA256SUMS)
          shasum -a 256 "$first/$archive" > "$RUNNER_TEMP/vellum-sdk.sha256"
          cp "$first/$archive" "$RUNNER_TEMP/$archive"
          cp scripts/install.sh "$RUNNER_TEMP/install.sh"
          cp scripts/install_core.py "$RUNNER_TEMP/install_core.py"
          (
            cd "$RUNNER_TEMP"
            shasum -a 256 \
              "$archive" \
              install.sh \
              install_core.py \
              | LC_ALL=C sort > SHA256SUMS
            shasum -a 256 -c SHA256SUMS
          )
          python3 scripts/verify_sdk_artifact.py \
            --archive "$RUNNER_TEMP/$archive" \
            --checksums "$RUNNER_TEMP/SHA256SUMS" --json \
            > "$RUNNER_TEMP/sdk-verification.json"
          python3 scripts/validate_installed_sdk.py \
            --archive "$RUNNER_TEMP/$archive" \
            --checksums "$RUNNER_TEMP/SHA256SUMS" \
            --forbid-path "$GITHUB_WORKSPACE" --json \
            > "$RUNNER_TEMP/sdk-installed-validation.json"

      - name: Verify the binary deployment boundary
        shell: bash
        run: |
          set -euo pipefail
          dylib=build-gpu/graphics/libvellum-gpu.dylib
          app=build-gpu/apps/gpu-native/vellum-gpu-native.app/Contents/MacOS/vellum-gpu-native
          otool -l "$dylib" > "$RUNNER_TEMP/vellum-gpu-dylib-load-commands.txt"
          otool -l "$app" > "$RUNNER_TEMP/vellum-gpu-app-load-commands.txt"
          /usr/bin/grep -Eq 'minos 15\.0' "$RUNNER_TEMP/vellum-gpu-dylib-load-commands.txt"
          /usr/bin/grep -Eq 'minos 15\.0' "$RUNNER_TEMP/vellum-gpu-app-load-commands.txt"

      - name: Record private-release trust controls
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: |
          set -euo pipefail
          test "$(git cat-file -t "$GITHUB_REF_NAME")" = tag
          test "$(git rev-parse "$GITHUB_REF_NAME^{commit}")" = "$GITHUB_SHA"
          git -c gpg.ssh.allowedSignersFile="$GITHUB_WORKSPACE/provenance/release-signers.allowed_signers" \
            verify-tag "$GITHUB_REF_NAME"
          test "$(
            ssh-keygen -lf provenance/release-signers.allowed_signers |
              awk '{ print $2 }'
          )" = SHA256:eMlEuZcnW6NJDrKQBuN5EX1q69bOlIRPl6XmutVTgcs
          VELLUM_TAG_OBJECT_SHA="$(git rev-parse "$GITHUB_REF_NAME")"
          export VELLUM_TAG_OBJECT_SHA
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          evidence = {
              "schema": "vellum.release-trust.v1",
              "repository": os.environ["GITHUB_REPOSITORY"],
              "source_commit": os.environ["GITHUB_SHA"],
              "tag": os.environ["GITHUB_REF_NAME"],
              "tag_object_sha": os.environ["VELLUM_TAG_OBJECT_SHA"],
              "trusted_release_signer": {
                  "principal": "daniel.raffel@gmail.com",
                  "ssh_fingerprint": (
                      "SHA256:eMlEuZcnW6NJDrKQBuN5EX1q69bOlIRPl6XmutVTgcs"
                  ),
              },
              "github_actions_build_attestation": {
                  "status": "unavailable",
                  "reason_code": "private-repository-enterprise-cloud-required",
                  "documentation": (
                      "https://docs.github.com/en/code-security/getting-started/"
                      "github-security-features#artifact-attestations"
                  ),
              },
              "github_immutable_release_attestation": {
                  "status": "required",
                  "documentation": (
                      "https://docs.github.com/en/code-security/concepts/"
                      "supply-chain-security/immutable-releases"
                  ),
              },
              "retained_controls": [
                  "trusted-ssh-signed-annotated-tag-bound-to-source-commit",
                  "same-run-byte-repeatability-bound-to-source-commit",
                  "github-immutable-release-attestation",
                  "github-release-asset-digests",
                  "sha256sums",
                  "sterile-installed-sdk-validation",
              ],
          }
          Path(os.environ["RUNNER_TEMP"], "release-trust.json").write_text(
              json.dumps(evidence, indent=2, sort_keys=True) + "\n"
          )
          PY

      - name: Publish the verified private release
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          tag="$GITHUB_REF_NAME"
          test "$tag" = "v0.1.0"
          version="${tag#v}"
          test "$(python3 cli/vellum_cli.py --version)" = "vellum $version"
          python3 -c '
          import json, sys
          identity = json.load(open(sys.argv[1], encoding="utf-8"))
          expected = sys.argv[2]
          if identity.get("framework_version") != expected or identity.get("cli_version") != expected:
              raise SystemExit("release artifact identity does not match its immutable tag")
          ' "$RUNNER_TEMP/sdk-verification.json" "$version"
          test "$(
            gh api "repos/$GITHUB_REPOSITORY/immutable-releases" --jq .enabled
          )" = true
          gh release verify-asset --help >/dev/null
          notes="$RUNNER_TEMP/release-notes.md"
          cat > "$notes" <<'EOF'
          Private experimental Vellum SDK for the first independent-application validation.

          This release is intentionally limited to the documented macOS 15.0+ arm64
          native and browser-Wasm proof lanes. It is not a Pulp dependency or a
          broad platform support claim.
          EOF
          release_json="$RUNNER_TEMP/release.json"
          if gh api "repos/$GITHUB_REPOSITORY/releases/tags/$tag" \
              > "$release_json" 2>/dev/null; then
            release_state="$(
              python3 -c 'import json,sys; print("draft" if json.load(open(sys.argv[1]))["draft"] else "published")' \
                "$release_json"
            )"
          else
            gh release create "$tag" \
              --repo "$GITHUB_REPOSITORY" \
              --draft \
              --verify-tag \
              --title "Vellum 0.1.0 experimental SDK" \
              --notes-file "$notes"
            release_state=draft
          fi

          base_assets=(
            "vellum-sdk-0.1.0-darwin-arm64.tar.gz=$RUNNER_TEMP/vellum-sdk-0.1.0-darwin-arm64.tar.gz"
            "install.sh=$RUNNER_TEMP/install.sh"
            "install_core.py=$RUNNER_TEMP/install_core.py"
            "SHA256SUMS=$RUNNER_TEMP/SHA256SUMS"
            "sdk-verification.json=$RUNNER_TEMP/sdk-verification.json"
            "sdk-installed-validation.json=$RUNNER_TEMP/sdk-installed-validation.json"
            "web-payload.json=$RUNNER_TEMP/web-payload.json"
            "release-trust.json=$RUNNER_TEMP/release-trust.json"
          )
          verifier_args=()
          upload_args=()
          for asset in "${base_assets[@]}"; do
            verifier_args+=(--asset "$asset")
            upload_args+=("${asset#*=}")
          done

          if [ "$release_state" = draft ]; then
            gh api "repos/$GITHUB_REPOSITORY/releases/tags/$tag" \
              > "$release_json"
            previous_verification_id="$(
              python3 -c '
          import json, sys
          assets = json.load(open(sys.argv[1]))["assets"]
          matches = [str(row["id"]) for row in assets if row["name"] == "release-assets-verification.json"]
          if len(matches) > 1:
              raise SystemExit("duplicate release-assets-verification.json assets")
          print(matches[0] if matches else "")
          ' "$release_json"
            )"
            if [ -n "$previous_verification_id" ]; then
              gh api --method DELETE \
                "repos/$GITHUB_REPOSITORY/releases/assets/$previous_verification_id"
            fi
            gh release upload "$tag" "${upload_args[@]}" \
              --repo "$GITHUB_REPOSITORY" --clobber
            gh api "repos/$GITHUB_REPOSITORY/releases/tags/$tag" \
              > "$release_json"
            python3 scripts/verify_release_assets.py \
              --release-json "$release_json" --tag "$tag" \
              "${verifier_args[@]}" \
              --output "$RUNNER_TEMP/release-assets-verification.json"
            gh release upload "$tag" \
              "$RUNNER_TEMP/release-assets-verification.json" \
              --repo "$GITHUB_REPOSITORY" --clobber
            gh api "repos/$GITHUB_REPOSITORY/releases/tags/$tag" \
              > "$release_json"
            python3 scripts/verify_release_assets.py \
              --release-json "$release_json" --tag "$tag" \
              "${verifier_args[@]}" \
              --asset "release-assets-verification.json=$RUNNER_TEMP/release-assets-verification.json" \
              --output "$RUNNER_TEMP/release-assets-draft-verification.json"
            gh release edit "$tag" \
              --repo "$GITHUB_REPOSITORY" \
              --draft=false \
              --latest=false
          else
            gh release download "$tag" \
              --repo "$GITHUB_REPOSITORY" \
              --pattern release-assets-verification.json \
              --dir "$RUNNER_TEMP"
          fi

          gh release verify "$tag" --repo "$GITHUB_REPOSITORY"
          gh release verify-asset "$tag" \
            "$RUNNER_TEMP/vellum-sdk-0.1.0-darwin-arm64.tar.gz" \
            --repo "$GITHUB_REPOSITORY"
          gh api "repos/$GITHUB_REPOSITORY/releases/tags/$tag" \
            > "$RUNNER_TEMP/release-published.json"
          python3 scripts/verify_release_assets.py \
            --release-json "$RUNNER_TEMP/release-published.json" \
            --tag "$tag" "${verifier_args[@]}" \
            --asset "release-assets-verification.json=$RUNNER_TEMP/release-assets-verification.json" \
            --require-published --require-immutable \
            --output "$RUNNER_TEMP/release-assets-published-verification.json"

      - name: Upload retained GPU evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: vellum-gpu-macos-arm64-evidence
          path: |
            ${{ runner.temp }}/vellum-gpu-proof.png
            ${{ runner.temp }}/vellum-gpu-proof.txt
            ${{ runner.temp }}/vellum-gpu-proof.sha256
            ${{ runner.temp }}/vellum-gpu-dylib-load-commands.txt
            ${{ runner.temp }}/vellum-gpu-app-load-commands.txt
            ${{ runner.temp }}/vellum-sdk-0.1.0-darwin-arm64.tar.gz
            ${{ runner.temp }}/install.sh
            ${{ runner.temp }}/install_core.py
            ${{ runner.temp }}/SHA256SUMS
            ${{ runner.temp }}/vellum-sdk.sha256
            ${{ runner.temp }}/sdk-first.json
            ${{ runner.temp }}/sdk-second.json
            ${{ runner.temp }}/sdk-verification.json
            ${{ runner.temp }}/sdk-installed-validation.json
            ${{ runner.temp }}/web-payload.json
            ${{ runner.temp }}/release-trust.json
            ${{ runner.temp }}/release-assets-verification.json
            ${{ runner.temp }}/release-assets-draft-verification.json
            ${{ runner.temp }}/release-assets-published-verification.json
          if-no-files-found: error
          retention-days: 90
